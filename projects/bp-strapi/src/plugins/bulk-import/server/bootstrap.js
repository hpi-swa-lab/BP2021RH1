'use strict';

/**
 * The bootstrap function is called right after the plugin has registered.
 * Important: needs to be defined, even if empty
 */
/* eslint-disable no-unused-vars */
module.exports = ({ strapi }) => ({
  // execute some bootstrap code
});
