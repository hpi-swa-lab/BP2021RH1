'use strict';

/**
 * browse-root-collection service.
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::browse-root-collection.browse-root-collection');
