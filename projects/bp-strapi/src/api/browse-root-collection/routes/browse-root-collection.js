'use strict';

/**
 * browse-root-collection router.
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::browse-root-collection.browse-root-collection');
